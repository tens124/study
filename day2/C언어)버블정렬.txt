#include<stdio.h>

void align(int a[])
{
	int temp;
	int length = 배열 길이;		//C언어에서 인수로 전달되는 배열은 길이 정보를 포함하지 않음. 배열의 시작 주소값이 전달되기 때문
						//따라서 배열 길이를 알아내기 위한 방법이 여러 가지 있다
						//문자열 배열의  경우 상대적으로 쉬움. NULL문자를 찾아내기만 하면 되기 때문
						//이외의 배열은 배열의 길이를 매개변수를 통해 함께 전달하거나
						//구조체에 배열 길이에 대한 정보를 담아 함께 전송하거나
						//배열의 길이를 측정하기 위한 매크로를 정의하는 등의 방법들이 존재한다
	for(int i = 0; i<length; i++)
	{
		for(int j = 0;j<length-i;j++)
		{
			if(a[j]>a[j+1])
			{
				int temp = a[j];
				a[j] = a[j+1];
				a[j+1] = temp;
			}
		}
	}
}


버블정렬을 구현하는 함수. 바깥쪽의 for문이 몇회차인지를 나타내고, 안쪽의 for문을 통해 정렬을 실행. 다음 인덱스보다 큰 값이라면 서로의 자리를 바꾼다
이때, 첫 회차가 종료되면 가장 큰 값은 맨 끝에, 2회차가 종료되면 가장 큰 값은 맨 끝에서 두 번째 자리에 놓이는 동작이 회차가 끝날 때마다 반복되므로 안쪽 for문의 정렬 동작을 length-i를 통해 제한해줌. 회차가 진행될수록 비교횟수가 적어지게 됨